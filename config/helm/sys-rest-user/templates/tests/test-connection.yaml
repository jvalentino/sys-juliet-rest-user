apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sys-rest-user.fullname" . }}-test-connection"
  labels:
    {{- include "sys-rest-user.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sys-rest-user.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
